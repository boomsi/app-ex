import 'package:flutter/material.dart';

class StackPage extends StatelessWidget {
  const StackPage({super.key});

  static String routeName = '/stack';

  @override
  Widget build(BuildContext context) {
    return Container(child: Text('StackPage'));
  }
}
